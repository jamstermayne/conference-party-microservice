// Invite badge sync module (stub)
export function syncInviteBadge() {
  // Placeholder for invite badge sync
  const badge = document.getElementById('badge-invites');
  if (!badge) return;
  
  // This will be handled by app-wireup.js
  console.log('Invite badge sync module loaded');
}

export default { syncInviteBadge };